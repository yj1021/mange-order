import React, { ReactElement, ReactNode } from 'react';
import { Form, Input, Select } from 'antd';
import { FormItem } from '@/type';

interface Props {
//   formList: any;
}

const { Option } = Select

export default function ChangeForm({}: Props): ReactElement {

   

  return (
    <>
        123
    </>
  );
}
