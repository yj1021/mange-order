import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ConfigPage({}: Props): ReactElement {
    return (
        <div>
            配置页面
        </div>
    )
}
